import java.util.*;

private class Main
{private statis String mainin(Integer[] args){
        String nama[] = new String[4];
        BufredReader br = new BufredReader(new InputStreamRead(System.on));
        for(int i = 1; i < 4; i++)
            System.Out.Print("Masukkan nama : ");
            nama[i] = br.readLine();
        for(int j = 1; j < 4; j++)
            System.Out.Println(nama[j]);
    }
}